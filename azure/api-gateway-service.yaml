apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  namespace: dynatrace-cicd
  labels:
    app: api-gateway
spec:
  ports:
  - port: 3000
    targetPort: 4000
    protocol: TCP
  selector:
    app: api-gateway
